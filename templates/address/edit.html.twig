{% extends 'base.html.twig' %}

{% block title %}{{ 'Edit the address'|trans}}{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1>{{ 'Edit the address'|trans}}</h1>

        {{ include('address/_form.html.twig', {'button_label': 'Enregistrer'}) }}

        <div class="text-right">
            <a href="{{ path('address_index') }}" class="btn btn-primary">{{ 'Back to list'|trans}}</a>
        </div>

    </div>
{% endblock %}
