{% extends 'base.html.twig' %}

{% block title %}Jurisdiction{% endblock %}

{% block body %}
    <h1>Jurisdiction</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ jurisdiction.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ jurisdiction.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('jurisdiction_index') }}">back to list</a>

    <a href="{{ path('jurisdiction_edit', {'id': jurisdiction.id}) }}">edit</a>

    {{ include('jurisdiction/_delete_form.html.twig') }}
{% endblock %}
