<form method="post" action="{{ path('jurisdiction_delete', {'id': jurisdiction.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ jurisdiction.id) }}">
    <button class="btn">Delete</button>
</form>
