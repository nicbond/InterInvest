{% extends 'base.html.twig' %}

{% block title %}{{ 'Create new legal form'|trans}}{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1>{{ 'Create new legal form'|trans}}</h1>

        {{ include('jurisdiction/_form.html.twig') }}

        <div class="text-right">
            <a href="{{ path('jurisdiction_index') }}" class="btn btn-primary">{{ 'Back to list'|trans}}</a>
        </div>

    </div>
{% endblock %}
