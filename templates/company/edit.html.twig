{% extends 'base.html.twig' %}

{% block title %}{{ 'Edit Company'|trans}}{% endblock %}

{% block body %}
    <div class="container mt-4">
        <div class="text-primary" style="font-weight: bold;font-size: 4rem;"><h1>{{ 'Edit Company'|trans}}</h1></div>

        {{ include('company/_form.html.twig', {'button_label': 'Enregistrer'}) }}

        <div class="text-right">
            <a href="{{ path('company_index') }}" class="btn btn-primary">{{ 'Back to list'|trans}}</a>
        </div>

    </div>
{% endblock %}
